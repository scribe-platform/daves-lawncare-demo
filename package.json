{
  "name": "@scribe-cms/template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Starter site template pushed to new customer repos at provisioning time (Stream 7). Generates its Astro content-collection config from the repo's own .scribe.yml at build time, with zero runtime dependency on the Scribe service.",
  "scripts": {
    "prebuild": "node scripts/generate-config.mjs",
    "build": "astro build",
    "predev": "node scripts/generate-config.mjs",
    "dev": "astro dev",
    "build:fixture": "node scripts/apply-fixture.mjs && npm run build",
    "dev:fixture": "node scripts/apply-fixture.mjs && npm run dev",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "cookie": "2.0.1"
  },
  "devDependencies": {
    "yaml": "^2.6.0"
  }
}
