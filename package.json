{
  "name": "daves-lawncare-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dave's Lawn Care — bespoke site, managed with Scribe (content only; page code is custom to this repo, not the shared Scribe template).",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.3.3"
  }
}
