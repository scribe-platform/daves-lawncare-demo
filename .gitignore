# Generated at build time by scripts/generate-config.mjs — never commit.
/src/content.config.ts
/src/site-config.ts

# Per-customer data: created by the admin app (provisioning writes
# .scribe.yml; content authoring writes into content/) or by
# scripts/apply-fixture.mjs for local testing. Neither belongs in the
# template package itself.
/.scribe.yml
/content/

dist/
.astro/
node_modules/
