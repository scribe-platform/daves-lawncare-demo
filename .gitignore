# This repo is a bespoke site: src/content.config.ts is hand-authored here
# (not generated from .scribe.yml), and .scribe.yml plus content/ are the
# product — the admin app writes them and they belong in version control.

dist/
.astro/
node_modules/
